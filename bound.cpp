#include<bits/stdc++.h>
using namespace std;

int main()
{
    // map<pair<int, string>, int> m;
    // sort(m.begin(), m.end(), greater)
    // m[{2,"puspraj"}] = 2;
    // m[{3,"neeraj"}] = 3;
    // m[{4, "pankaj"}] = 4;
    // cout << m.at()
}

