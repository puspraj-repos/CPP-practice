#include<bits/stdc++.h>
using namespace std;

unordered_map<int, int> um;
stack<int> Stack;
vector<int> visited;
vector<vector<int>> graph;

int main()
{
    int n, u, v, edges;
    cin >> n;
    graph.resize()
    for(int i = 0; i < n; i++)
    {
        cin >> edges;
        cin >> v;

    }

}